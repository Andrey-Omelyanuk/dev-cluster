apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: gitea-tmp
spec:
  selector: 
    matchLabels: { app: gitea }
  storageClassName: manual
  accessModes: 
    - ReadWriteOnce
  resources: 
    requests: 
      storage: 20Gi
